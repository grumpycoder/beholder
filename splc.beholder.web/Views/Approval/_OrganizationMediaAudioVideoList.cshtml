@model IEnumerable<splc.domain.Models.OrganizationMediaAudioVideoRel>

<h2>Organization-Audio Video Relationships</h2>
<table class="table table-hover">
    <tr>
        <th>Organization
        </th>
        <th>Audio Video
        </th>
        <th>Date Start
        </th>
        <th>Date End
        </th>
        <th>Relationship Type
        </th>
        <th>Primary Status
        </th>
        <th>Approval Status
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Organization.OrganizationName
            </td>
            <td>
                @item.MediaAudioVideo.Title
            </td>
            <td>
                @item.DateStart
            </td>
            <td>
                @item.DateEnd
            </td>
            <td>
                @(item.RelationshipType == null ? "Unknown" : item.RelationshipType.Name)
            </td>
            <td>
                @(item.PrimaryStatusId == null ? "Unknown" : item.PrimaryStatus.Name)
            </td>
            <td>
                @item.ApprovalStatus.Name
            </td>
            <td class="nowrap">
                <a href="@Url.Action("ApproveOrganizationMediaAudioVideo", "Approval", new { id = item.Id })" 
                    class="btn btnApprove" 
                    data-title="Edit Relationship" 
                    data-refresh="true" 
                    data-action="@Url.Action("GetOrganizationMediaAudioVideoApprovals", "Approval")">Approve</a>

                <a href="@Url.Action("RejectOrganizationMediaAudioVideo", "Approval", new { id = item.Id })" 
                    class="btn btnApprove" 
                    data-title="Edit Relationship" 
                    data-refresh="true" 
                    data-action="@Url.Action("GetOrganizationMediaAudioVideoApprovals", "Approval")">Disapprove</a>

            </td>
        </tr>
    }

</table>

@model IEnumerable<splc.domain.Models.VehicleVehicleRel>

<h2>Vehicle-Vehicle Relationships</h2>
<table class="table table-hover">
    <tr>
        <th>VIN
        </th>
        <th>VIN
        </th>
        <th>Date Start
        </th>
        <th>Date End
        </th>
        <th>Relationship Type
        </th>
        <th>Primary Status
        </th>
        <th>Approval Status
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Vehicle.VIN
            </td>
            <td>
                @item.Vehicle2.VIN
            </td>
            <td>
                @item.DateStart
            </td>
            <td>
                @item.DateEnd
            </td>
            <td>
                @(item.RelationshipType == null ? "Unknown" : item.RelationshipType.Name)
            </td>
            <td>
                @(item.PrimaryStatusId == null ? "Unknown" : item.PrimaryStatus.Name)
            </td>
            <td>
                @item.ApprovalStatus.Name
            </td>
            <td class="nowrap">
                <a href="@Url.Action("ApproveVehicleVehicle", "Approval", new { id = item.Id })" 
                    class="btn btnApprove" 
                    data-title="Edit Relationship" 
                    data-refresh="true" 
                    data-action="@Url.Action("GetVehicleVehicleApprovals", "Approval")">Approve</a>

                <a href="@Url.Action("RejectVehicleVehicle", "Approval", new { id = item.Id })" 
                    class="btn btnApprove" 
                    data-title="Edit Relationship" 
                    data-refresh="true" 
                    data-action="@Url.Action("GetVehicleVehicleApprovals", "Approval")">Disapprove</a>

            </td>
        </tr>
    }

</table>

@model IEnumerable<splc.domain.Models.Prefix>
<h3>Prefixes</h3>
<table class="table table-hover">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Sort Order</th>
        </tr>
    </thead>
    @foreach (var item in @Model)
    {
        <tr>
            <td class="nowrap">
                <a href="@Url.Action("EditPrefix", "Admin", new { id = item.Id })" 
                    class="btnEdit" 
                    data-title="Edit Prefix" 
                    data-refresh="true" 
                    data-action="@Url.Action("Prefix", "Admin", null)"><span class="glyphicon glyphicon-pencil"></span></a>
                <a href="@Url.Action("DeletePrefix", "Admin", new { id = item.Id })" 
                    class="btnDelete" 
                    data-title="Delete Prefix" 
                    data-refresh="true" 
                    data-action="@Url.Action("Prefix", "Admin", null)"><span class="glyphicon glyphicon-trash"></span></a>
            </td>
            <td>@item.Name</td>
            <td>@item.SortOrder</td>
        </tr>

    }
    <tr>
        <td colspan="3">
            <a href="@Url.Action("CreatePrefix", "Admin", null)" 
                class="btnNew" 
                data-refresh="true" 
                data-title="New Prefix" 
                data-action="@Url.Action("Prefix", "Admin", null)"><span class="glyphicon glyphicon-plus"></span></a>
        </td>
    </tr>
</table>


@model IEnumerable<splc.domain.Models.VehicleMake>
<h3>Vehicle Makes</h3>
<table class="table table-hover">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Sort Order</th>
        </tr>
    </thead>
    @foreach (var item in @Model)
    {
        <tr>
            <td class="nowrap">
                <a href="@Url.Action("EditVehicleMake", "Admin", new { id = item.Id })" 
                    class="btnEdit" 
                    data-title="Edit Vehicle Make" 
                    data-refresh="true" 
                    data-action="@Url.Action("VehicleMake", "Admin", null)"><span class="glyphicon glyphicon-pencil"></span></a>
                <a href="@Url.Action("DeleteVehicleMake", "Admin", new { id = item.Id })" 
                    class="btnDelete" 
                    data-title="Delete Vehicle Make" 
                    data-refresh="true" 
                    data-action="@Url.Action("VehicleMake", "Admin", null)"><span class="glyphicon glyphicon-trash"></span></a>
            </td>
            <td>@item.Name</td>
            <td>@item.SortOrder</td>
        </tr>

    }
    <tr>
        <td colspan="3">
            <a href="@Url.Action("CreateVehicleMake", "Admin", null)" 
                class="btnNew" 
                data-refresh="true" 
                data-title="New Vehicle Make" 
                data-action="@Url.Action("VehicleMake", "Admin", null)"><span class="glyphicon glyphicon-plus"></span></a>
        </td>
    </tr>
</table>


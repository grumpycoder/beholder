@model IEnumerable<splc.domain.Models.VehicleModel>
<h3>Vehicle Models</h3>
<table class="table table-hover">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Sort Order</th>
        </tr>
    </thead>
    @foreach (var item in @Model)
    {
        <tr>
            <td class="nowrap">
                <a href="@Url.Action("EditVehicleModel", "Admin", new { id = item.Id })" 
                    class="btnEdit" 
                    data-title="Edit Vehicle Model" 
                    data-refresh="true" 
                    data-action="@Url.Action("VehicleModel", "Admin", null)"><span class="glyphicon glyphicon-pencil"></span></a>
                <a href="@Url.Action("DeleteVehicleModel", "Admin", new { id = item.Id })" 
                    class="btnDelete" 
                    data-title="Delete Vehicle Model" 
                    data-refresh="true" 
                    data-action="@Url.Action("VehicleModel", "Admin", null)"><span class="glyphicon glyphicon-trash"></span></a>
            </td>
            <td>@item.Name</td>
            <td>@item.SortOrder</td>
        </tr>

    }
    <tr>
        <td colspan="3">
            <a href="@Url.Action("CreateVehicleModel", "Admin", null)" 
                class="btnNew" 
                data-refresh="true" 
                data-title="New Vehicle Model" 
                data-action="@Url.Action("VehicleModel", "Admin", null)"><span class="glyphicon glyphicon-plus"></span></a>
        </td>
    </tr>
</table>


@model IEnumerable<splc.domain.Models.EyeColor>
<h3>Eye Colors</h3>
<table class="table table-hover">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Sort Order</th>
        </tr>
    </thead>
    @foreach (var item in @Model)
    {
        <tr>
            <td class="nowrap">
                <a href="@Url.Action("EditEyeColor", "Admin", new { id = item.Id })" 
                    class="btnEdit" 
                    data-title="Edit Eye Color" 
                    data-refresh="true" 
                    data-action="@Url.Action("EyeColor", "Admin", null)"><span class="glyphicon glyphicon-pencil"></span></a>
                <a href="@Url.Action("DeleteEyeColor", "Admin", new { id = item.Id })" 
                    class="btnDelete" 
                    data-title="Delete Eye Color" 
                    data-refresh="true" 
                    data-action="@Url.Action("EyeColor", "Admin", null)"><span class="glyphicon glyphicon-trash"></span></a>
            </td>
            <td>@item.Name</td>
            <td>@item.SortOrder</td>
        </tr>

    }
    <tr>
        <td colspan="3">
            <a href="@Url.Action("CreateEyeColor", "Admin", null)" 
                class="btnNew" 
                data-refresh="true" 
                data-title="New Eye Color" 
                data-action="@Url.Action("EyeColor", "Admin", null)"><span class="glyphicon glyphicon-plus"></span></a>
        </td>
    </tr>
</table>


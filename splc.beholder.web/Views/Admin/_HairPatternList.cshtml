@model IEnumerable<splc.domain.Models.HairPattern>
<h3>Hair Patterns</h3>
<table class="table table-hover">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Sort Order</th>
        </tr>
    </thead>
    @foreach (var item in @Model)
    {
        <tr>
            <td class="nowrap">
                <a href="@Url.Action("EditHairPattern", "Admin", new { id = item.Id })" 
                    class="btnEdit" 
                    data-title="Edit Hair Pattern" 
                    data-refresh="true" 
                    data-action="@Url.Action("HairPattern", "Admin", null)"><span class="glyphicon glyphicon-pencil"></span></a>
                <a href="@Url.Action("DeleteHairPattern", "Admin", new { id = item.Id })" 
                    class="btnDelete" 
                    data-title="Delete Hair Pattern" 
                    data-refresh="true" 
                    data-action="@Url.Action("HairPattern", "Admin", null)"><span class="glyphicon glyphicon-trash"></span></a>
            </td>
            <td>@item.Name</td>
            <td>@item.SortOrder</td>
        </tr>

    }
    <tr>
        <td colspan="3">
            <a href="@Url.Action("CreateHairPattern", "Admin", null)" 
                class="btnNew" 
                data-refresh="true" 
                data-title="New Hair Pattern" 
                data-action="@Url.Action("HairPattern", "Admin", null)"><span class="glyphicon glyphicon-plus"></span></a>
        </td>
    </tr>
</table>


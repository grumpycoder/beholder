@model IEnumerable<AddressType>
<h3>Address Types</h3>
<table class="table table-hover">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Sort Order</th>
        </tr>
    </thead>
    @foreach (var item in @Model)
    {
        <tr>
            <td class="nowrap">
                <a href="@Url.Action("EditAddressType", "Admin", new { id = item.Id })" 
                    class="btnEdit" 
                    data-title="Edit Address Type" 
                    data-refresh="true" 
                    data-action="@Url.Action("AddressType", "Admin", null)"><span class="glyphicon glyphicon-pencil"></span></a>
                <a href="@Url.Action("DeleteAddressType", "Admin", new { id = item.Id })" 
                    class="btnDelete" 
                    data-title="Delete Address Type" 
                    data-refresh="true" 
                    data-action="@Url.Action("AddressType", "Admin", null)"><span class="glyphicon glyphicon-trash"></span></a>
            </td>
            <td>@item.Name</td>
            <td>@item.SortOrder</td>
        </tr>

    }
    <tr>
        <td colspan="3">
            <a href="@Url.Action("CreateAddressType", "Admin", null)" 
                class="btnNew" 
                data-refresh="true" 
                data-title="New Address Type" 
                data-action="@Url.Action("AddressType", "Admin", null)"><span class="glyphicon glyphicon-plus"></span></a>
        </td>
    </tr>
</table>


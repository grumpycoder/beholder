@model OrganizationComment

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<div class="row">
    @Html.Partial("_CreateOrEditOrganizationComment", Model)
    <input type="submit" value="Save" />
</div>

<div>
    @Html.ActionLink("Back", "Details", "Organizations", new { id = Model.OrganizationId }, null)
</div>

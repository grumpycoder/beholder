@model MediaAudioVideo

<div class="span4 card">
    <div class="title">
        @Html.ActionLink(string.Format("{0}", Model.Title), "Details", "MediaAudioVideos", new { id = Model.Id }, null)
    </div>
    <div>
@*        @if (Model.Image != null)
        {
            <div><img src="@Url.Action("ShowImage", "MediaAudioVideos", new { Id = Model.Image.Id })" alt="media image" class="person-thumbnail" /></div>
        }
        else
        {
            *@<div>
                <img src="~/content/img/unknown-person.png" class="person-thumbnail" alt="no image" />
            </div>
@*        }*@
    </div>
    <div>
        <table class="table">
            <tr>
                <td>Type</td>
                <td>@(Model.MediaType == null ? "Unknown" : Model.MediaType.Name)</td>
            </tr>
            <tr>
                <td>Movement</td>
                <td>@(Model.MovementClass == null ? "Unknown" : Model.MovementClass.Name)</td>
            </tr>
            <tr>
                <td>Date Received/Recorded</td>
                <td>@(Model.DateReceivedRecorded == null ? "Unknown" : @String.Format("{0:d}", Model.DateReceivedRecorded))</td>
            </tr>
            <tr>
                <td>Date Aired</td>
                <td>@(Model.DateAired == null ? "Unknown" : @String.Format("{0:d}", Model.DateAired))</td>
            </tr>
        </table>
    </div>    
</div>
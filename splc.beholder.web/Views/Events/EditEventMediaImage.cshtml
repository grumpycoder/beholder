@model EventMediaImageRel

@{
    ViewBag.Title = "Edit";
}
<h2 class="page-title small">Edit Event/MediaImage Relationship</h2>

@using (Html.BeginForm(null, null, FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.ValidationSummary(true)
    <fieldset class="form-horizontal well well-small fieldset-auto-width">
        <legend></legend>

        @Html.HiddenFor(model => model.Id)
        @Html.Partial("_CreateOrEditEventMediaImage", Model)

        <input type="submit" value="Save Relationship" class="btn btn-primary pull-right" />
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
